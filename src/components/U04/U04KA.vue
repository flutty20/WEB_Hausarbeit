<template>
  <div>
    <p class="frage">Über welche Art der Typisierung verfügt die Programmiersprache JavaScript?</p>
    <p class="antwort">Tim Berners-Lee</p>

    <p class="frage">Was ist der Datentyp des typeof-Operators?</p>
    <p class="antwort">Tim Berners-Lee</p>

    <p class="frage">Was ist der Datentyp von [1,2,3]?</p>
    <p class="antwort">Array(Objekt)</p>

    <p class="frage">Was ergibt der JavaScript-Ausdruck 0.1+0.2==0.3 ? Begründen Sie Ihr Ergebnis!</p>
    <p class="antwort">false</p>

    <p class="frage">Welchen Wert ergibt folgender Ausdruck: a = 3; a == '3'</p>
    <p class="antwort">true</p>

    <p class="frage">Welchen Wert ergibt folgender Ausdruck: false === false</p>
    <p class="antwort">true</p>

    <p class="frage">Geben Sie ein Beispiel, für das "==" intransitiv ist.</p>
    <p class="antwort">Tim Berners-Lee</p>

    <p class="frage">Wie greift man auf alle Argumente einer Funktion zu?</p>
    <p class="antwort">Tim Berners-Lee</p>

    <p class="frage">Wie kann man die Liste aller Argumente einer Funktion in ein Array umwandeln?</p>
    <p class="antwort">Tim Berners-Lee</p>

    <p
      class="frage"
    >Schreiben Sie ein JavaScript-Objekt x, das ein Zahlenpaar (a,b) speichert, dessen Summe die Methode des Objektes x.sum() liefert.</p>
    <p class="antwort">Tim Berners-Lee</p>

    <p
      class="frage"
    >Schreiben Sie eine Fabrikfunktion, die ein Counter-Objekt mit zwei Methoden inc() und dec() berechnet, mit denen man den Counter hoch- und herunter-zählen kann.</p>
    <p class="antwort">Tim Berners-Lee</p>

    <p class="frage">Ergänzen Sie folgende Funktionen mit filter-map-reduce ...</p>
    <p class="antwort">Tim Berners-Lee</p>

    <p class="frage">Schreiben Sie einen Stack (eine Queue) in JavaScript und PHP.</p>
    <p class="antwort">Tim Berners-Lee</p>

    <p
      class="frage"
    >Wie heißt das Design Pattern in JavaScript (ES5), das die Kapselung innerer Variablen erlaubt? 1. Beschreiben Sie dieses Design Pattern! 2. Wenden Sie das Design Pattern an, um einen Counter zu implementieren, der inkrementiert werden kann (+1) und dessen Zählerstand man abfragen, aber dessen Zählerstand nicht anderweitig manipuliert werden kann (außer durch die Inkrementierung).</p>
    <p class="antwort">Tim Berners-Lee</p>

    <p class="frage">
      (1) Schreiben Sie eine JavaScript-Funktion, die ein JavaScript-Objekt x erzeugt, das ein Zahlenpaar (a,b) speichert, dessen Produkt die Methode des Objektes x.product() liefern soll. Beide Werte, a und b, sollen geheim gehalten werden, d.h. weder a noch b sollen lesbar sein. Außerdem soll a, aber nicht b veränderbar (überschreibbar) sein.
      <br />(2) Ist die Geheimhaltung von a und b sicher? Schreiben Sie eine JavaScript-Funktion disclosure(x), die die beiden Werte a und b trotzdem indirekt berechnen kann. Diese Funktion soll die Werte von a und b am Ende unverändert lassen.
    </p>
    <p class="antwort">Tim Berners-Lee</p>

    <p
      class="frage"
    >Schreiben Sie eine Fabrikfunktion triple, die eine zweitstellige Funktion in eine einstellige umwandelt. Bei einer Anwendung auf die Multiplikation soll damit eine Kubusfunktion x3 berechnet werden.</p>
    <p class="antwort">Tim Berners-Lee</p>

    <p class="frage">
      (1) Wie ist in JavaScript die reduce()-Methode definiert?
      <br />(2) Schreiben Sie mit reduce() eine Funktion compose , die beliebig viele einstellige Funktionen, die als Parameter übergeben werden, nacheinander aufeinander anwendet. Z.B. soll compose(square, square)(3) das Ergebnis 81 ergeben, da 32 = 9 und 92 = 81 ergibt.
    </p>
    <p class="antwort">Tim Berners-Lee</p>
  </div>
</template>

<script>
export default {
  name: "",
  props: {},
};
</script>

<!-- Add "scoped" attribute to limit CSS to this component only -->
<style scoped>
</style>